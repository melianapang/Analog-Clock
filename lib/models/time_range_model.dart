//Model class for difference time object for barchart when click the notification.
class TimeRange {
  final String? payload;
  final int diffTimeOpen;

  TimeRange(this.payload, this.diffTimeOpen);
}
